<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="3dp"
    app:strokeWidth="0dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Imagen -->
        <ImageView
            android:id="@+id/ivFavImage"
            android:layout_width="100dp"
            android:layout_height="0dp"
            android:scaleType="centerCrop"
            android:src="@drawable/bg_hero_landscape"
            android:contentDescription="Imagen de la actividad"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Badge novedad -->
        <TextView
            android:id="@+id/tvNewBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="8dp"
            android:background="#FFEB3B"
            android:paddingHorizontal="8dp"
            android:paddingVertical="3dp"
            android:text="¡Precio actualizado!"
            android:textColor="#212121"
            android:textSize="10sp"
            android:textStyle="bold"
            android:visibility="gone"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/ivFavImage" />

        <!-- Título -->
        <TextView
            android:id="@+id/tvFavTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="44dp"
            android:layout_marginTop="14dp"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="#212121"
            android:maxLines="2"
            android:ellipsize="end"
            app:layout_constraintTop_toBottomOf="@id/tvNewBadge"
            app:layout_constraintStart_toEndOf="@id/ivFavImage"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Categoría -->
        <TextView
            android:id="@+id/tvFavCategory"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="6dp"
            android:background="#E3F2FD"
            android:paddingHorizontal="8dp"
            android:paddingVertical="3dp"
            android:textColor="#1565C0"
            android:textSize="11sp"
            app:layout_constraintTop_toBottomOf="@id/tvFavTitle"
            app:layout_constraintStart_toEndOf="@id/ivFavImage" />

        <!-- Precio -->
        <TextView
            android:id="@+id/tvFavPrice"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="6dp"
            android:layout_marginBottom="14dp"
            android:textColor="#2E7D32"
            android:textSize="15sp"
            android:textStyle="bold"
            app:layout_constraintTop_toBottomOf="@id/tvFavCategory"
            app:layout_constraintStart_toEndOf="@id/ivFavImage"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- Botón reservar rápido -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnFavReserve"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="8dp"
            android:text="Reservar"
            android:textSize="12sp"
            android:textAllCaps="false"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- Botón quitar favorito -->
        <ImageButton
            android:id="@+id/btnUnfavorite"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:background="@android:color/transparent"
            android:src="@drawable/ic_favorite_filled"
            android:contentDescription="Quitar de favoritos"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
